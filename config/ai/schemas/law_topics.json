{
    "version": 1,
    "type": "object",
    "additionalProperties": false,
    "required": ["topics"],
    "properties": {
        "topics": {
            "type": "array",
            "minItems": 1,
            "maxItems": 6,
            "description": "Topic slugs from the taxonomy that describe what this law is about.",
            "items": {"type": "string"}
        },
        "confidence": {
            "type": "number",
            "minimum": 0,
            "maximum": 1
        }
    }
}
